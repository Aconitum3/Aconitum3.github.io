services:
  astro:
    build:
      context: .
    container_name: astro
    ports:
      - 4321:4321
    develop:
      watch:
        - action: sync
          path: .
          target: /home/app
          ignore:
            - node_modules/
        - action: rebuild
          path: package.json

